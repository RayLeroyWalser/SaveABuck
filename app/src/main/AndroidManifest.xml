<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saveabuck"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.caux.saveabuck.MainActivity"
            android:label="@string/title_activity_menu_principal" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.caux.saveabuck.AddTransactionActivity"
            android:label="@string/title_activity_menu_principal"
            android:theme="@style/NoBarDialog" >
        </activity>
        <activity
            android:name="com.caux.saveabuck.AddGroupActivity"
            android:label="@string/title_activity_add_group"
            android:theme="@style/NoBarDialog" >
        </activity>
    </application>

</manifest>
